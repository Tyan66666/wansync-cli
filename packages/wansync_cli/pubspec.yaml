name: wansync_cli
description: WanSync CLI —— 在服务器/终端上导入 App 导出的配置 JSON 并同步 OneLap 活动到各平台。
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.3

dependencies:
  args: ^2.5.0
  dio: ^5.7.0
  json5: ^0.8.2
  sync_core:
    path: ../sync_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  wansync: wansync
resolution: workspace
